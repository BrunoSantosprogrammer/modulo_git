function soma(numero1: number, numero2: number): number{
    return numero1 + numero2;
}
console.log(soma(20, 10))

function nomes (nome: string): string {
    return `"Olá ${nome}"`;
}

console.log(nomes("Bruno"))